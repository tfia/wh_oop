#include<iostream>
#include<stdio.h>
#include<list>
#include<string>
#include<unordered_map>
#include<set>
#include"thm.h"

int main()
{
    freopen("test.in", "r", stdin);
    freopen("test.out", "w", stdout);
    theorem a;
    std::cin >> a;
    std::cout << a;
    return 0;
}